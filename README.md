# BrowserPilot Tap Review (Secondary Distribution Path)

This repository is retained only as a secondary distribution path.
Canonical buyer entry is `https://browserpilot.seedsource.dev/install`. Homebrew remains a secondary or recovery distribution path and should not lead buyer onboarding copy.

## Current State
- Formula file is present in Formula/browserpilot-bootstrap.rb.
- Remote is configured to `https://github.com/browser-pilot/homebrew-tap.git`.
- No push/publish actions were executed.
- This repo should trail the BrowserPilot install flow and any approved bootstrap release, not lead them.

## Secondary Review
Validate this repo only after the npm release, install-page copy, and activation-server release artifact are already aligned.

## Publish Is Manual (Not Performed)
Only after review:
1. Commit reviewed files.
2. Push intentionally.
